<Window x:Class="Sanduhr.App.Views.TextPromptWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sanduhr"
        Height="180" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#0d0d0d">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TitleText" Text="Rename account"
                   FontSize="15" FontWeight="Bold" Foreground="#17d4fa" />
        <TextBlock Grid.Row="1" x:Name="PromptText" Text="New name" FontSize="11"
                   Foreground="#b8b4ac" Margin="0,14,0,4" />
        <TextBox Grid.Row="2" x:Name="ValueBox" FontSize="13" Padding="8,6"
                 Background="#1c1c1c" Foreground="#e8e4dc" BorderBrush="#333333" CaretBrush="#e8e4dc" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Padding="16,7" Margin="0,0,8,0"
                    Background="#1c1c1c" Foreground="#e8e4dc" BorderBrush="#5A6982" BorderThickness="1"
                    Cursor="Hand" IsCancel="True" Click="OnCancelClick" />
            <Button Content="Save" Padding="20,7" IsDefault="True"
                    Background="#17d4fa" Foreground="#0d0d0d" BorderThickness="0"
                    FontWeight="Bold" Cursor="Hand" Click="OnSaveClick" />
        </StackPanel>
    </Grid>
</Window>
