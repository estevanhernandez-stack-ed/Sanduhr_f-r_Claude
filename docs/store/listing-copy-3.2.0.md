# Store listing copy — v3.2.0

Public-facing copy for the Partner Center listing. Distinct from the **Notes for certification**
(that's [`reviewer-letter-3.2.0.0.md`](./reviewer-letter-3.2.0.0.md), reviewer-only).

The **Description** below is the cert-passed v3.1 copy, kept verbatim except for the 3.2 deltas
called out in `> NOTE` lines (the Claude Code bullet grew into the Claude Usage tab; PRIVACY BY
DESIGN gains the vault sentence). The **disclaimer is unchanged** from the approved listing — do
not reword trademark language on a resubmission.

---

## What's new in this version

```
New: the Claude Usage tab — the full picture of your local Claude Code usage,
in Settings.

- Overview: today and last-30-days totals, now split into sent and received
  tokens, with a 30-day bar strip and a rolling five-week calendar. Click any
  day on the calendar to jump straight to that day's sessions.
- Sessions: every session with its project, models, and token burn — agent
  runs fold into the session that spawned them. Stack by project, sort any
  column, export CSV.
- Trends: weekly usage bars, up to 26 weeks back.
- Optional history vault: Claude Code deletes its own logs after about 30
  days. Opt in and Sanduhr keeps your usage totals — never conversation
  content — on your machine, so your trends survive. Pause per folder or
  erase everything, any time, in Settings.
- Sharper widget: each tier's percentage now sits on its own chip and stays
  readable on every theme and sparkline style.

Everything else — burn-rate projection, the pace ghost, themes,
multi-account, the focus timer — carries over unchanged.

Independent third-party tool. Sanduhr für Claude is not affiliated with,
endorsed by, or associated with Anthropic PBC. "Claude" and "claude.ai" are
trademarks of Anthropic PBC, used nominatively to describe what this tool
integrates with.
```

---

## Short description (~200 char — shows in search results)

Unchanged from the approved 3.1 listing:

```
Know when you'll hit your Claude weekly cap before you do. A native Windows 11 glass
widget that turns your claude.ai usage into burn-rate, pace, and 30-day history at a glance.
```

---

## Description (full — cert-passed v3.1 copy + 3.2 deltas)

```
Know when you'll hit your Claude weekly cap — before you do.

Sanduhr für Claude is a native Windows 11 glass widget for Claude Pro, Team, and
Enterprise users. It turns your claude.ai usage into something you can actually pace
yourself by — not a mirror of the numbers on the settings page, but the analysis
layer on top.

WHY PEOPLE KEEP IT OPEN

• Burn-rate projection — "You'll hit your weekly cap in ~4h 22m at current pace."
  Know before you run dry.
• Pace ghost on every bar — a tick shows where you should be right now. Sit left of
  it and you're under pace, right of it and you're ahead. Pace by eye, no math.
• The Claude Usage tab — your local Claude Code sessions turned into a dashboard:
  today and 30-day totals split into sent and received tokens, weekly trends, a
  clickable usage calendar, and a sessions ledger with per-project stacking and
  CSV export. A live token-burn delta stays on every tier card and in the footer,
  with daily-quota tracking for the Routines tier.
• Optional usage history vault — Claude Code deletes its own logs after ~30 days;
  opt in and Sanduhr keeps your usage totals (never conversation content) on your
  machine so your trends survive. Pause or erase it any time.
• 2-hour horizon sparkline — velocity trend inline on every bar, denser than a line
  chart at the same pixel budget.
• Deep-work focus timer — swap the tier cards for a digitised pixel hourglass that
  drains in real time when you want to lock in.
• Cooldown snake game — for when you've burned through your budget and need to kill a
  few minutes.
• Five hand-tuned glass themes — Obsidian, Aurora, Ember, Mint, Matrix — on a Win11
  Mica backdrop (solid-color fallback on Win10). Author your own too: paste a JSON
  palette, or hand the built-in AI-agent prompt to Claude (or any LLM) with a
  reference image and drop the result straight in.
• Multi-account support — track Personal and Work Claude accounts in one install,
  each with its own history, and switch from the widget.
• 30-day local history with CSV export — see your usage trend over time and analyze
  it anywhere.
• Built to live on your desktop — every feature one click away on the bottom tool
  strip; drag from anywhere, pin always-on-top, compact mode, edge-drag resize.
  Window position and theme persist between sessions. Full keyboard surface:
  Ctrl+R refresh, Ctrl+, settings, Ctrl+D compact, Ctrl+H help.

WHAT YOU NEED

Your own active Claude Pro, Team, or Enterprise subscription. On first run, sign in
on the real Claude page right inside the app — no DevTools needed. (Signing in with
Google? Google blocks that inside apps, so Sanduhr walks you through a quick one-time
session-key paste, step by step.) That's it.

PRIVACY BY DESIGN

Your credentials live only in Windows Credential Manager (service com.626labs.sanduhr)
and are wiped when you uninstall. No account is created with 626Labs. No server, no
telemetry, no analytics, no ads. Sanduhr reads claude.ai — and your local Claude Code
session logs — using your own session; nothing about your usage ever leaves your
machine. The optional history vault is opt-in, stores usage totals only (never
conversation content), lives in local AppData, and can be erased from Settings at
any time.

—

Independent third-party tool. Sanduhr für Claude is not affiliated with, endorsed by,
or associated with Anthropic PBC. "Claude" and "claude.ai" are trademarks of Anthropic
PBC, used nominatively to describe what this tool integrates with.

Built by 626 Labs for Claude power users who want to pace themselves, not just track.
```

> Deltas from the approved v3.1 description:
> 1. **Claude Code bullet → Claude Usage tab bullet** — the old "Live token-burn from your local
>    Claude Code sessions" bullet grew into the tab description (totals + split + trends +
>    calendar + ledger); the live tier-card delta and Routines daily-quota sentences are carried
>    inside it verbatim-in-spirit so nothing approved was dropped.
> 2. **New vault bullet** — the one genuinely new data surface, sold honestly: opt-in, totals
>    only, local, erasable.
> 3. **PRIVACY BY DESIGN** — appended the vault sentence (opt-in / totals only / local AppData /
>    erasable). The pre-existing "nothing about your usage ever leaves your machine" claim
>    remains true and unchanged.
>
> Everything else — including the disclaimer, WHAT YOU NEED, and the closing line — is the
> approved copy, untouched.

---

## Keywords (search terms)

Unchanged from 3.1, plus "token tracker":

```
claude, claude.ai, usage, usage tracker, rate limit, token usage, token tracker,
AI usage, widget, desktop widget, burn rate, focus timer, productivity
```

## Screenshots to capture (adds to the 3.1 set)

1. Default widget — rings/bars + the bottom tool strip visible (carry over if current)
2. **Claude Usage ▸ Overview — the split lines + bar strip + calendar (NEW, lead screenshot
   candidate)**
3. **Claude Usage ▸ Sessions — stacked projects with an expanded group (NEW)**
4. A theme swap or vivid theme applied
5. The Horizon graph mode with the % chip visible on a tier card
6. The focus-timer hourglass

Minimum 3, ideally 6. 1366×768 or larger; PNG. The two NEW shots are the release's story —
capture them on a real corpus with the vault on (numbers can be real; no conversation content
appears anywhere in these surfaces).
